# =============================================================================
# DEV CONFIG - Local development entry point
# =============================================================================
# Flash locally with: esphome run dev.yaml
# =============================================================================

substitutions:
  name: "espcontrol-waveshare-7b-dev"
  friendly_name: "EspControl Waveshare 7B Dev"
  espcontrol_component_url: "../.."
  espcontrol_component_ref: "HEAD"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

web_server:
  js_url: ""
  # Reuse the existing Guition web UI bundle until a dedicated Waveshare bundle is added.
  js_include: "../../docs/public/webserver/guition-esp32-p4-jc1060p470/www.js"

packages:
  espcontrol: !include packages.yaml
